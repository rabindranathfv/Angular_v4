export interface infoPageInterface {
  // el simbolo de ? indica q el campo es opcion y evita el error en la definicion al usar la interfaz
  titulo?: string;
  email?: string;
  pagina_autor?: string;
  facebook?: string;
  twitter?: string;
  team_work?: any[];
}
